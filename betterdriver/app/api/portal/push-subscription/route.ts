import { NextRequest, NextResponse } from "next/server";
import { getSession } from "@/lib/auth";
import { supabaseAdmin } from "@/lib/supabase";
export async function POST(req:NextRequest){const s=await getSession();if(!s)return NextResponse.json({error:"Unauthorized"},{status:401});const b=await req.json();const subscription=b.subscription;if(!subscription||typeof subscription.endpoint!=="string")return NextResponse.json({error:"A valid browser subscription is required"},{status:400});const {error}=await supabaseAdmin.from("driver_push_subscriptions").upsert({driver_id:s.driverId,endpoint:subscription.endpoint,subscription,enabled:true,updated_at:new Date().toISOString()},{onConflict:"endpoint"});if(error)return NextResponse.json({error:"Could not save notification preference"},{status:500});return NextResponse.json({ok:true});}
export async function DELETE(req:NextRequest){const s=await getSession();if(!s)return NextResponse.json({error:"Unauthorized"},{status:401});const endpoint=new URL(req.url).searchParams.get("endpoint");if(!endpoint)return NextResponse.json({error:"Endpoint required"},{status:400});await supabaseAdmin.from("driver_push_subscriptions").update({enabled:false,updated_at:new Date().toISOString()}).eq("driver_id",s.driverId).eq("endpoint",endpoint);return NextResponse.json({ok:true});}
