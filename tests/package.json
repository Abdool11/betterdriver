{
  "name": "tag-ecosystem-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test tests/api/ --reporter=list",
    "test:ui": "playwright test tests/ui/ --reporter=list",
    "test:e2e": "playwright test tests/e2e/ --reporter=list",
    "test:all": "playwright test --reporter=html",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^25.8.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.1",
    "dotenv": "^17.4.2",
    "jose": "^6.2.3"
  }
}
